# VELA Project Wrapper

This folder is a VELA starter package copied into a research project by `vela init`.

VELA gives Codex a bounded, evidence-aware operating layer. It separates materials,
evidence, claims, methods, deliverables, handoffs, logs, and machine-readable state.

Start by reading `AGENTS.md`, then create a small handoff under `handoffs/`.
