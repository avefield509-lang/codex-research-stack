# Claims Directory Rules

Claims are not final findings by default.

Codex must keep unsupported claims marked as `candidate` or `needs_evidence`, and must never mark a claim as supported without linked evidence IDs.
