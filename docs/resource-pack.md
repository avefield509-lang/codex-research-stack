# Resource Pack

VELA public packages should include only declared workflow resources and sanitized examples.

Each bundled file should have a clear purpose and privacy class. Public packages must exclude private notes, local machine paths, credentials, caches, compiled bytecode, and workstation-specific environment scripts.

HELM may package its own desktop resources separately. That does not change VELA's requirement to remain portable.
